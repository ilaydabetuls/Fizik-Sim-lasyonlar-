{
  "name": "fizik-simulasyonlari",
  "version": "1.0.0",
  "description": "Fizik Simülasyonları Araştırması",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run preview"
  },
  "workspaces": [
    "frontend"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
